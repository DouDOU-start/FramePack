# Core PyTorch and Deep Learning
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
einops>=0.7.0

# Diffusers and Transformers
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.20.0
safetensors>=0.4.0

# Hugging Face Hub
huggingface-hub>=0.20.0

# Image and Video Processing
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Gradio Web Interface
gradio>=4.0.0

# Progress and UI
tqdm>=4.65.0

# Optional: Flash Attention (for better performance)
# flash-attn>=2.0.0  # Uncomment if you have compatible GPU and want better performance

# Optional: Sage Attention (alternative attention implementation)
# sageattention>=1.0.0  # Uncomment if available and want to try alternative attention

# System utilities
psutil>=5.9.0

# JSON and file handling (usually included with Python)
# json - built-in
# os - built-in
# sys - built-in
# argparse - built-in
# tempfile - built-in
# shutil - built-in
# glob - built-in
# datetime - built-in
# math - built-in
# threading - built-in
# warnings - built-in
# logging - built-in
# traceback - built-in

# Note: FFmpeg is required for video processing but needs to be installed separately
# Windows: Download from https://ffmpeg.org/download.html
# Linux: sudo apt install ffmpeg (Ubuntu/Debian) or sudo yum install ffmpeg (CentOS/RHEL)
# macOS: brew install ffmpeg
